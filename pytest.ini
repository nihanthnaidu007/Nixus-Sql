[pytest]
asyncio_mode = auto
# tests/ = the offline unit suite (the default gate); eval/ = the live-server
# benchmark harness, which SKIPS cleanly when the API/DB are unreachable
# (eval/conftest.py probes infra once per session). Bare `pytest` therefore
# collects both and passes offline.
testpaths = tests eval
timeout = 300
